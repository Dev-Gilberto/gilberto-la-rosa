---
interface Props {
  date: Date;
}
const { date } = Astro.props;
---

<time datetime={date ? date.toISOString() : null} class="capitalize">
  publicación: <span class="uppercase">
    {
      date
        ? date.toLocaleDateString("es", {
            year: "numeric",
            month: "short",
            day: "numeric",
          })
        : null
    }</span
  >
</time>
